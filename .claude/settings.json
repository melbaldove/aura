{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "agent",
            "if": "Bash(git commit*)",
            "model": "haiku",
            "prompt": "A commit is about to be made. Check if man pages in docs/man/ need updating.\n\n1. Run `git diff --cached --name-only` to see what files are being committed.\n2. If any files in src/aura/acp/, src/aura/brain.gleam, src/aura/brain_tools.gleam, src/aura/scheduler.gleam, src/aura/config.gleam, src/aura/supervisor.gleam, src/aura/skill.gleam, src/aura/structured_memory.gleam, src/aura/shell.gleam, or src/aura/conversation.gleam changed, read the relevant man page:\n   - aura-flares.7 for ACP/flare changes\n   - aura-config.5 for config/env changes\n   - aura-diagnostics.7 for error handling changes\n   - aura.1 for architecture changes\n3. Compare the staged changes against what the man page says. Is it still accurate?\n4. If man pages are already in the staged files, check that the updates are consistent with the code changes.\n\nReturn {\"ok\": true} if man pages are accurate or don't need updating (e.g. test-only changes, comments, formatting).\nReturn {\"ok\": false, \"reason\": \"<which man page section needs updating and why>\"} if a man page is now stale.",
            "timeout": 30,
            "tools": ["Bash", "Read", "Grep"]
          }
        ]
      }
    ]
  }
}
